# NapMysqlTool 2.x 任务分解

> 任务依赖关系：阶段一 → 阶段二 → 阶段三/四（可并行） → 阶段五
> 阶段三和阶段四可交叉进行，UI重构和实例管理功能相互关联
> UI设计已确认，详见 `设计原型.html`

## 阶段一：数据层改造

### T1: config.ini结构重构
- 将现有的 `[mysql配置]` + `[工具配置]` 两section结构改为多实例section + `[工具配置]`
- 每个实例section名称格式为 `[实例-{实例名}]`，包含：mysql路径、mysql账号、mysql密码、端口、数据目录、ini文件、导出的库名
- `[工具配置]` 保留：主题、多语言，去掉"初始化脚本路径"和"导出的库名"（移到实例级别）
- 编写config.ini的向后兼容读取（检测旧格式 `[mysql配置]` 自动转换为新格式 `[实例-默认]`）
- 修改 `MysqlUtils.readIniFile` / `writeIniFile` 适配新结构
- 实例section识别规则：section名以 `实例-` 开头即为实例配置，`工具配置`为全局配置

### T2: 实例数据模型
- 新建 `MysqlInstance` 类，封装单个实例的所有配置字段：
  - `name`（实例名称，即section名去掉"实例-"前缀）
  - `mysqlPath`（mysql路径）
  - `mysqlUser`（mysql账号）
  - `mysqlPass`（mysql密码）
  - `port`（端口，int类型）
  - `dataDir`（数据目录）
  - `iniFile`（ini文件路径，可选）
  - `exportDbs`（导出的库名，多个用空格分割）
- `MysqlInstance` 持有运行时状态：`running`（是否在运行）、`pid`（当前PID）
- 持有一个 `List<MysqlInstance>` 在 `MysqlToolApp` 中替代原来的 `iniProp`
- 提供从config.ini解析实例列表、写入实例列表到config.ini的方法
- 工具配置单独存储（主题、多语言），与实例列表分开

## 阶段二：操作层改造

### T3: MysqlOperator多实例适配
- 所有方法（start/stop/hasPid/isStarted/initSql/outDb/fix/killPid）改为接收 `MysqlInstance` 参数，不再用全局 `Map<String, Map<String, String>>`
- `start(MysqlInstance)` 方法：
  - 有ini文件：`mysqld.exe --defaults-file={iniFile} --port={port} --console`
  - 无ini文件：`mysqld.exe --port={port} --datadir={dataDir} --console`
  - 注意：`--defaults-file` 必须是第一个参数（参考commit 0873bc0的修复）
- `stop(MysqlInstance)` 方法：`mysqladmin.exe -u{user} -p{pass} -P{port} shutdown`（加上 `-P` 参数指定端口）
- `initSql(MysqlInstance, String db, String file)`：`cmd.exe /C mysql.exe -u{user} -p{pass} -P{port} {db} < {file}`（加上 `-P`）
- `outDb(MysqlInstance)`：`cmd.exe /C mysqldump.exe -u{user} -p{pass} -P{port} --databases {dbs} --hex-blob > output-{port}.sql`
- `fix(MysqlInstance)`：使用实例的dataDir路径
- `hasPid(MysqlInstance)`：使用实例的mysqlPath查找PID文件
- `killPid(MysqlInstance)`：使用实例的mysqlPath查找PID文件，杀进程
- `isStarted(MysqlInstance)`：JDBC连接使用实例的端口 `jdbc:mysql://localhost:{port}/mysql`

### T4: PID验证与端口检测
- 新增 `isPidValid(MysqlInstance)` 方法：读取PID文件中的进程号，通过 `tasklist /FI "PID eq X" /FI "IMAGENAME eq mysqld.exe"` 检查该PID是否是一个真实运行的mysqld进程
- PID无效时直接删除PID文件，不询问用户
- 新增 `isPortOccupied(int port)` 方法：通过 `netstat -ano` 检查端口占用情况
- 新增 `getPortOccupierPid(int port)` 方法：返回占用指定端口的进程PID
- 新增 `isOurInstance(MysqlInstance, int pid)` 方法：比对PID是否属于我们自己管理的实例
- 启动流程改造：
  1. 检查PID文件 → 若PID无效（进程不存在或不是mysqld），直接删除PID文件
  2. 检查端口 → 若端口被占用，判断是否自己的实例
  3. 若外部占用 → 弹窗提示用户确认，确认后杀进程，拒绝则放弃启动
  4. 正常启动

## 阶段三：UI重构

### T5: 主界面重设计
- 窗口尺寸扩大至约560x500，适配多实例列表展示
- 顶部标题栏：左侧窗口标题，右侧模拟3个窗口控制按钮（圆点）
- 导航栏：左侧3个Toggle标签（启停功能/其他功能/ini管理），右侧"+ 添加实例"按钮 + 主题切换图标
- 中部改为实例列表区域（ScrollPane + VBox）
- 底部：仅版本号，不再显示"MySQL启停工具 by Nap"（与标题栏重复）
- 配色风格：黑白灰极简，按钮统一灰底灰字细边框，语义色仅用在状态标签上。详见 `设计原型.html`
- `NapTheme` 类继续管理主题切换，新增样式变量支持（通过JavaFX CSS自定义属性或常量）

### T6: 启停功能页
- 顶部："全部启动"（绿色语义）+ "全部停止"（红色语义）按钮，占满一行
- 每个实例一行（HBox），从左到右依次排列：
  - 状态点（Circle，半径4，颜色：绿=已启动/红=未启动/蓝=启动中/黄=端口冲突）
  - 实例名（Label，粗体13px）
  - 端口标签（Label样式，等宽数字，54px最小宽度）
  - 状态标签（Label样式：已启动/未启动/启动中/端口冲突，带语义色边框和文字）
  - 操作按钮（启动/停止/重启/删除×）：统一灰底灰字，disabled时opacity 0.3
- 全部启动时逐个启动，等前一个就绪（检测到ready for connections）后启动下一个
- 操作期间禁用相关按钮，完成后恢复

### T7: 其他功能页
- 每个实例一行（与启停页同一HBox布局）：状态点 + 实例名 + 端口标签 + 状态标签 + 操作按钮（修复/导入/导出）
- 未启动实例的导入/导出按钮disabled
- 导出文件名：`output-{端口}.sql`
- 导入文件名：`output-{端口}.sql`
- 修复操作：先停止再修复数据目录

### T8: ini管理界面（详见 `设计原型.html` 的 ini管理 标签页）
- 独立标签页（非其他功能页内），HBox(侧边列表 + 右侧编辑区)
- 侧边列表（200px宽，可滚动）：每个实例一行，右侧显示"有ini"/"无"徽章
- 右侧编辑区分为上下两部分：
  - **常用参数表单**（Grid 2列，最多140px高，超出滚动）：datadir、max_connections、character_set_server、innodb_buffer_pool_size等，TextField数组，修改后实时同步到下方TextArea
  - **ini全文TextArea**：占满剩余高度，支持自由编辑，修改后同步更新表单中对应字段
- 双向同步策略：表单→文本（直接生成ini文本），文本→表单（解析[mysqld]下的key=value，匹配到的key回填表单）
- 底部操作按钮：删除ini + 保存
- 创建新ini时工具自动填入port和datadir作为模板
- ini文件存放于实例mysql路径目录下

## 阶段四：实例管理

### T9: 添加/删除实例UI
- 提供添加实例按钮，弹出对话框填写：
  - 实例名称（必填）
  - mysql路径（必填，支持绝对和相对路径）
  - 端口（必填，int）
  - mysql账号（必填）
  - mysql密码（可选，允许空字符串）
  - 数据目录（必填，支持绝对和相对路径）
  - ini文件（可选，留空则不使用ini）
- 添加后立即写入config.ini并刷新界面
- 提供删除实例按钮：删除前弹窗确认，若实例正在运行则先停止再删除
- 删除后立即更新config.ini并刷新界面
- 导出的库名也需要配置，从 `[工具配置]` 移到每个实例自己的section里

### T10: 初始化脚本按实例隔离
- 每个实例的init目录命名为 `init-{端口}` 或 `init-{实例名}`
- 每个实例有独立的已初始化列表文件 `已初始化列表-{端口}.txt`
- 启动时只执行对应实例目录下的脚本

## 阶段五：收尾

### T11: 关闭时自动停止
- `MysqlToolApp.stop()` 中遍历所有实例，逐个停止
- 停止失败时用killPid兜底
- 保存工具配置到config.ini

### T12: 版本更新与测试
- 更新版本号
- 测试：单实例启停、多实例同时启停、端口冲突场景、PID过期场景、ini管理、导入导出