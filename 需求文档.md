# NapMysqlTool 2.x 需求文档

## 设计决策记录

以下为需求讨论过程中确认的关键决策：

| 决策项 | 结论 | 原因 |
|--------|------|------|
| 实例数量 | 动态多实例，不限数量 | 用户可能在不同场景使用不同MySQL版本 |
| ini文件与端口的关系 | 端口始终由config.ini控制，通过命令行 `--port` 传入；ini文件可选，用于其他MySQL参数 | 命令行参数优先级最高，避免ini没配端口时多个实例冲突（MySQL默认3306）；两处都配端口会造成优先级混乱；规则：端口看config.ini，其他看ini文件 |
| ini管理UI方式 | 常用参数表单 + 自由文本编辑，双向实时同步 | 类似CC switch，表单方便普通用户，文本区方便高级用户 |
| 端口冲突处理 | 弹窗提示用户确认后杀掉外部进程 | 自动杀进程太激进（可能杀系统服务），仅提示又不解决问题 |
| PID过期处理 | 自动检测并直接清理，不询问用户 | PID过期是明确的异常状态（进程不存在或不是mysqld），无需用户决策；只有端口冲突时才需要用户确认 |
| PID过期检测方式 | `tasklist /FI "PID eq X" /FI "IMAGENAME eq mysqld.exe"` | 需同时验证进程存在且是mysqld，因为重启后PID可能被其他程序复用 |
| 数据目录 | 支持绝对路径和相对路径 | 开发时用绝对路径指向打包目录，部署时用相对路径 |
| 初始化脚本 | 每个实例独立init目录 | 多实例的脚本互不影响 |
| 导入导出 | 按实例隔离，文件名用端口区分 | 防止多实例导出互相覆盖 |
| 关闭工具时 | 自动停止所有已启动实例 | 防止遗留进程占用端口 |

## R1: 多实例MySQL管理

### 背景
当前只支持单个MySQL实例。需要支持同时启动多个不同版本的MySQL（如5.7和8），它们端口不同，需要用ini指定端口并持久化配置。

### 需求

#### R1.1 动态实例管理
- 支持动态添加/删除MySQL实例，数量不限
- 每个实例独立配置，config.ini按实例分区存储

#### R1.2 config.ini结构改造
每个实例一个section，格式如下：
```
[实例-冒险岛5.7]
mysql路径=mysql-5.7.40
mysql账号=root
mysql密码=root
端口=3306
数据目录=data5.7
ini文件=
导出的库名=beidou
```
- `端口`：启动时始终通过命令行 `--port=X` 传入，优先级最高，覆盖ini中的端口设置
- `数据目录`：支持绝对路径和相对路径（相对mysql路径）
- `ini文件`：可选。留空时只用命令行参数；指定时通过 `--defaults-file` 加载，ini文件用于端口以外的MySQL参数（buffer_size、max_connections等）
- 启动命令规则：
  - 无ini：`mysqld.exe --port=3307 --datadir=xxx --console`
  - 有ini：`mysqld.exe --defaults-file=mysql.ini --port=3307 --console`

#### R1.3 ini管理UI
- 提供界面管理每个实例的MySQL ini文件
- 双模式编辑：
  - **常用参数表单**：列出常用MySQL参数（datadir、max_connections、character_set_server、innodb_buffer_pool_size等），用户填写后实时同步到自由文本区域
  - **自由文本编辑**：直接编辑ini全文，想加什么参数都行，适合高级用户
- 表单修改实时反映到文本区，文本区修改也能自由操作，两区域始终同步
- 可创建新ini（工具自动填入port和datadir作为模板）
- ini文件存放在实例mysql路径下

#### R1.4 初始化脚本按实例隔离
- 每个实例有自己的init目录（如 `init-3306/`）
- 脚本只对对应实例执行，互不影响
- 已执行列表也按实例分开记录

#### R1.5 导入导出按实例隔离
- 导出时针对选中实例的库
- 文件名按端口区分：`output-3306.sql`、`output-3307.sql`
- 导入同理，从对应文件导入到对应实例

---

## R2: 启停界面改造

### 背景
当前界面（400x150）只有一个按钮组，无法容纳多实例的启停操作。

### 需求

#### R2.1 一键启停所有
- 提供"全部启动"和"全部停止"按钮
- 全部启动时按实例逐个启动，等待前一个就绪后再启动下一个（避免同时初始化脚本冲突）

#### R2.2 单个实例启停
- 每个实例有独立的启动/停止按钮
- 支持单个实例的重启

#### R2.3 实例状态展示
- 每个实例实时显示运行状态（已启动/未启动/启动中）
- 端口冲突时显示警告状态

#### R2.4 关闭时自动停止
- 工具关闭时，自动停止所有已启动的MySQL实例
- 在config.ini中保存当前主题等工具级配置

---

## R3: PID与端口检测增强

### 背景
当前根据PID文件判断启停状态，存在两种异常场景。

### 需求

#### R3.1 PID文件过期自动清理（无需用户确认）
- 场景：关机后PID文件残留，但对应进程已不存在，导致误判为"已启动"
- 处理：启动前检测PID文件中的进程号，**过期则直接清理**，不弹窗询问
- 检测方式：`tasklist /FI "PID eq X" /FI "IMAGENAME eq mysqld.exe"`
  - 无匹配结果 → PID过期（进程不存在，或PID被其他程序复用），直接删除PID文件，继续启动
  - 有匹配结果 → mysqld确实在运行，PID有效

#### R3.2 端口占用检测与处理
- 场景a：系统安装的MySQL开机自启占用了3306，而我们的实例配置也是3306，导致启动失败但PID判断显示"未启动"，反复重试无果
- 处理：
  1. 启动前检测目标端口是否已被占用
  2. 若被占用，判断占用进程是否是我们自己管理的实例（比对PID文件）
  3. 若非自己管理的外部进程，弹窗提示用户："端口3306被外部进程(PID=XXX)占用，是否终止该进程？"
  4. 用户确认后终止外部进程，继续启动
  5. 用户拒绝则放弃启动，提示端口冲突
- 检测方式：通过 `netstat -ano` 找到占用端口的PID，或JNA调用

---

## R4: 界面设计确认（详见 `设计原型.html`）

### R4.1 整体布局
- 窗口带标题栏（模拟系统窗口样式，3个圆点按钮）
- 导航栏：左侧3个标签页（启停功能 / 其他功能 / ini管理），右侧"+ 添加实例"按钮 + 主题切换图标
- 内容区：根据标签页切换
- 底部：仅显示版本号

### R4.2 启停功能页
- 顶部：全部启动 + 全部停止 两个按钮，占满一行
- 每个实例一行，从左到右排列：状态点（圆点）→ 实例名 → 端口标签 → 状态标签 → 操作按钮（启动/停止/重启/删除）
- 状态标签：已启动（绿色边框+文字）、未启动（红色）、启动中（蓝色）、端口冲突（黄色）
- 删除按钮默认半透明，hover时更明显

### R4.3 其他功能页
- 每个实例一行：状态点 → 实例名 → 端口标签 → 状态标签 → 操作按钮（修复/导入/导出）

### R4.4 ini管理页
- 左侧：实例列表（200px宽），每个实例右侧显示"有ini"/"无"徽章
- 右侧上半：常用参数表单，Grid 2列，最多140px高，超出可滚动
- 右侧下半：ini全文文本区，占满剩余高度
- 底部：删除ini + 保存按钮

### R4.5 配色风格
- 极简黑白灰，所有按钮统一灰底灰字细边框
- 语义色仅用在状态标签和状态点的文字/边框上（绿/红/蓝/黄）
- 暗色/亮色双主题，JavaFX可通过 `-fx-style` 动态切换

### R4.6 对话框
- 添加实例对话框：8个字段（实例名称、mysql路径、端口、账号、密码、数据目录、导出库名、ini文件）
- 端口冲突对话框：提示端口号、占用进程信息、用户确认是否终止