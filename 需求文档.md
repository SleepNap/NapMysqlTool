# NapMysqlTool 3.x 需求文档

## 整体布局

```
┌──────────────────────────────────────────────────┐
│  [启停功能] [扩展功能] [实例管理]        [+] [☀]  │  ← Tab栏
├──────────────────────────────────────────────────┤
│                                                  │
│              根据Tab切换内容                      │  ← 主体
│                                                  │
├──────────────────────────────────────────────────┤
│  3.26.0525                                       │  ← 版本号
└──────────────────────────────────────────────────┘
```

- 顶部：3个Tab（ToggleButton选中高亮） + 添加实例 + 主题切换
- 中间：按Tab切换加载对应内容
- 底部：版本号
- 支持亮/暗主题、中英文国际化

---

## 1. 启停功能

### 界面

```
┌──────────────────────────────────────────────────┐
│  [  启动全部  ] [  停止全部  ] [  重启全部  ]     │
├──────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────┐   │
│  │ MySQL8.0             3306  ● 已启动      │   │
│  │                     [启动] [停止] [重启]  │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │ MySQL5.7             3307  ● 未启动      │   │
│  │                     [启动] [停止] [重启]  │   │
│  └──────────────────────────────────────────┘   │
│  ...（可滚动）                                   │
└──────────────────────────────────────────────────┘
```

### 全部操作按钮

| 按钮 | 说明 | 禁用条件 |
|------|------|----------|
| 启动全部 | 依次启动所有实例 | 全部已启动时禁用 |
| 停止全部 | 依次停止所有实例 | 全部未启动时禁用 |
| 重启全部 | 依次重启所有实例 | 有实例启动中时禁用 |

### 实例卡片

- 卡片左上：实例名称
- 卡片右上：端口号 + 状态（带颜色圆点：绿=已启动，红=未启动，灰=启动中）
- 卡片右下：启动 / 停止 / 重启 按钮

单实例按钮禁用逻辑：
- 已启动 → "启动"禁用
- 未启动 → "停止"禁用
- 启动中 → 全部禁用

### 后端操作

- 启动：`mysqld` 命令行，`.pid` 文件跟踪进程
- 停止：`mysqladmin shutdown`，失败后 `taskkill /f /pid`
- 重启：先停后启
- 最多重试3次
- 状态通过 JDBC 连接确认

---

## 2. 扩展功能

界面与启停功能布局一致，操作按钮替换为修复/导入/导出。

### 全部操作按钮

| 按钮 | 说明 |
|------|------|
| 修复全部 | 依次修复所有实例 |
| 导入全部 | 依次导入所有实例 |
| 导出全部 | 依次导出所有实例 |

### 单实例操作

| 按钮 | 说明 |
|------|------|
| 修复 | `mysqlcheck --auto-repair` |
| 导入 | 选择.sql文件，`cmd.exe /C mysql < file.sql` |
| 导出 | 选择保存路径，`mysqldump` 导出，`cmd.exe /C` 执行 |

按钮状态依赖实例运行状态（未启动时修复/导入/导出均禁用）。

---

## 3. 实例管理

改动最大。原来的ini管理（双模式编辑器）替换为实例管理，编辑ini改为弹窗方式。

### 界面

```
┌──────────────────────────────────────────────────┐
│  ┌──────────────────────────────────────────┐   │
│  │ MySQL8.0             3306  ● 已启动      │   │
│  │                     [修改] [删除]        │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │ MySQL5.7             3307  ● 未启动      │   │
│  │                     [修改] [删除]        │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  [  编辑INI  ]                                   │
└──────────────────────────────────────────────────┘
```

### 添加实例（顶部 + 按钮）

弹出对话框，填写：实例名称、MySQL路径、用户名、密码、端口、数据库名，确认后写入 `config.ini`，刷新界面。

### 修改实例

弹出对话框，预填当前实例配置，修改后更新 `config.ini`，刷新界面。

### 删除实例

确认对话框，确认后从 `config.ini` 移除对应 section，刷新界面。

### 编辑INI（弹窗维护）

- 弹出新窗口，内嵌文本区域，直接显示 `config.ini` 原始内容
- 允许手动编辑全文
- 保存时做基本格式校验（section头、key=value格式）
- 保存后重新加载配置并刷新界面
- 弹窗内应有警告提示，避免误操作

---

## 4. 配置文件 (`config.ini`)

```ini
; 工具配置(Tool Configuration)
[工具配置]
; 语言，zh-CN中文，en-US英文(Language, zh-CN or en-US)
language=en-US
; 主题颜色，dark深色，light浅色(Theme Color, dark or light)
theme=dark

; 实例名称(Instance Name)
[MySQL8.0]
; 实例路径(Instance Path)
path=mysql-8.0.39-winx64
; 用户名(Username)
username=root
; 密码(Password)
password=root
; 端口(Port)
port=3306
; 数据库(Database)
database=beidou
```

- 首个 section 为 `[工具配置]`，存放语言和主题
- 后续每个 section 为一个MySQL实例，section名即实例名称
- INI key 使用英文，读写兼容中英文 key
- 注释 `;` 开头，附带中英文说明

---

## 5. 数据模型

```
ToolConfig
├── Core
│   ├── section: Info<String>     // 工具配置
│   ├── language: Info<String>    // zh-CN | en-US
│   └── theme: Info<String>       // dark | light
└── List<Instance>
    └── Instance
        ├── section: Info<String>  // 实例名称（section名）
        ├── path: Info<String>     // MySQL目录
        ├── username: Info<String> // 用户名
        ├── password: Info<String> // 密码
        ├── port: Info<String>     // 端口
        ├── database: Info<String> // 数据库名
        └── status: int           // 0=未启动, 1=已启动, 2=启动中
```

每个 `Info<T>` 包含 `data`（值）、`sort`（排序序号）、`comment`（注释）。

---

## 6. 关键决策

| 决策 | 结论 | 原因 |
|------|------|------|
| 实例数量 | 不限，动态管理 | 用户可能使用不同MySQL版本 |
| 端口冲突 | 待定 | 自动杀进程太激进，仅提示不解决问题 |
| PID过期 | 自动检测清理 | 明确的异常状态，无需用户决策 |
| 关闭工具 | 自动停止所有实例 | 防止遗留进程 |
| ini管理方式 | 弹窗文本编辑 | 简化设计，高级用户直接用文本编辑 |
| 多实例启停 | 依次操作 | 避免同时初始化造成资源冲突 |